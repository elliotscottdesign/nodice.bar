import BookingSettingsClient from "./BookingSettingsClient";

export const metadata = {
  title: "Booking switches — No Dice Admin",
};

export default function BookingSettingsPage() {
  return <BookingSettingsClient />;
}
